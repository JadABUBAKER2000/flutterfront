name: unibook
description: UniBook - limited shop (auth & profile local DB)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  sqflite: ^2.2.8+4
  path_provider: ^2.0.15
  path: ^1.8.3
  crypto: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
    - assets/images/placeholder.png
